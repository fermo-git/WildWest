// Cambia SERVER_IP a la IP local de tu laptop (la que aparece en ipconfig / ifconfig).
// Ambos teléfonos deben estar en la misma red WiFi que la laptop.
export const SERVER_URL = 'http://192.168.1.7:3001';
